void prueba() {
    const uno = 1;
    var variable = 5;
    var c = 4 * 2 - 18 / 3 * 3;
     print "resutado de 4 * 2 - 18 / 3 * 3 = ", c, "\n";
    print "Comienzo del programa\n";
    while (variable) {
        variable = variable - uno;
        if (variable) print "no se ha llegado a cero, variable: " , variable, "\n";
        else print "Se ha llegado a cero\n";
    }

    print "Final del programa\n";

}